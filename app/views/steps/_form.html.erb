<%= form_for(@step, remote: true) do |f| %>
  <div class="field">
    <%= f.label :steps_count %>
    <%= f.text_field :count %>
  </div>
  <div class="field">
    <%= f.label :date %>
    <%= f.date_field :date %>
  </div>
  <button id="form-submit">Submit</button>
<% end %>
